<%= form_for(@supply) do |f| %>
  <% if @supply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supply.errors.count, "error") %> prohibited this supply from being saved:</h2>

      <ul>
      <% @supply.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
 <% end %>
<table class="gridtable">
<tr>
<td>   <%= f.label :name %></td>
<td>    <%= f.text_field :name %></td>
</tr>
<tr>
<td>    <%= f.label :amount %></td>
<td>    <%= f.text_field :amount %></td>
</tr>
<tr>
<td>    <%= f.label :party_id %></td>
<td>        <%= f.collection_select :party_id, Party.find(:all),:id, :name, {:prompt => "Select Party"},{:selected => @supply.party_id} %></td>

</tr>
<tr>
<td>    <%= f.label :user_id %></td>
<td>    <%= f.collection_select :user_id, User.find(:all),:id, :email, {:prompt => "Select User"}, {:selected => @supply.user_id} %></td>

</table>

<div class="actions">
    <%= f.submit %>
  </div>


<% end %>
