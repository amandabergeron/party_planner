<%= form_for(@party) do |f| %>
  <% if @party.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@party.errors.count, "error") %> prohibited this party from being saved:</h2>

      <ul>
      <% @party.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<center><table class="gridtable">
<tr>
<td>    <%= f.label :user_id %></td>
<td>    <%= f.collection_select :user_id, User.find(:all),:id, :email, {:prompt => "Select User"},{ :selected => @party.user_id} %></td>
</tr>
<tr>
<td>    <%= f.label :name %></td>
<td>    <%= f.text_field :name %></td>
</tr>
<tr>
<td>    <%= f.label :location %></td>
<td>    <%= f.text_field :location %></td>
</tr>
<tr>
<td>    <%= f.label :start %></td>
<td>    <%= f.time_select :start %></td>
</tr>
<tr>
<td>    <%= f.label :end %></td>
<td>    <%= f.time_select :end %></td>
</tr>
<tr>
<td>    <%= f.label :description %></td>
<td>    <%= f.text_area :description, :rows =>6 %></td>
</tr>
<tr>
<td>    <%= f.label :dress %></td>
<td>    <%= f.text_field :dress %></td>
</tr>
<tr>
<td>    <%= f.label :date %></td>
<td>    <%= f.date_select :date %></td>
</tr>

</table></center>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
